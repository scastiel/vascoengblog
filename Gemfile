source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# Pin the Sass converter to the 2.x line: it uses `sassc` (native, arm64-friendly)
# rather than 3.x's `sass-embedded`, which ships an x86_64-only protobuf binary
# that breaks on Apple Silicon.
gem "jekyll-sass-converter", "~> 2.0"
